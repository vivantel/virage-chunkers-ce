## Summary

<!-- What does this PR do? Which package(s) does it touch? -->

## Changes

<!-- Bullet list of notable changes -->

## Test plan

- [ ] `npm run type-check` passes
- [ ] `cargo clippy -- -D warnings` passes
- [ ] `cargo fmt --check` passes
- [ ] `npm test` passes
- [ ] New/changed chunker: `createChunker(opts)` exports verified
- [ ] `ChunkMeta` required fields populated on all emitted chunks
- [ ] Fixture test covers at least one real file of the target format

## Notes

<!-- Anything a reviewer should know: performance impact, breaking changes, follow-ups -->
